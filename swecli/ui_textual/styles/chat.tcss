/* SWE-CLI Chat Application Styles - Redesigned for Elegance */
/* Inspired by Claude Code: minimal, professional, clean */

/* ============================================
   COLOR PALETTE - Muted and Professional
   ============================================ */

Screen {
    background: #1e1e1e;  /* Neutral dark gray (was harsh blue) */
}

/* ============================================
   MAIN LAYOUT
   ============================================ */

#main-container {
    height: 100%;
    layout: vertical;
}

/* ============================================
   CONVERSATION AREA - Borderless and Clean
   ============================================ */

#conversation {
    height: 1fr;
    border: none;                /* ✨ NO BORDER - Clean look! */
    background: #1e1e1e;         /* Neutral background */
    padding: 1 2;                /* More generous padding */
    scrollbar-gutter: stable;
}

#conversation:focus {
    background: #252526;         /* Subtle depth change on focus */
    border: none;                /* Still no border */
}

/* ============================================
   INPUT AREA - Minimal and Elegant
   ============================================ */

#input-container {
    height: auto;
    layout: vertical;
    background: #1e1e1e;
}

#input-label {
    height: 1;
    content-align: left middle;
    color: #808080;              /* Muted gray (was bright cyan) */
    background: #1e1e1e;
    padding: 0 2;
}

/* Input field - borderless with subtle focus */
#input {
    height: 3;
    max-height: 10;
    min-height: 3;
    border: none;                /* ✨ NO BORDER by default */
    background: #252526;         /* Subtle depth */
    padding: 1 2;
    color: #cccccc;              /* Softer white */
}

#input:focus {
    background: #2d2d2d;         /* Elevated background on focus */
    border-left: thick #007acc;  /* ✨ Subtle left accent only */
}

/* ============================================
   STATUS BAR - Clean and Professional
   ============================================ */

#status-bar {
    height: 1;
    background: #252526;         /* Slightly elevated */
    color: #808080;              /* Muted gray */
    padding: 0 2;
    content-align: left middle;
}

/* ============================================
   TEXT AREA WIDGETS
   ============================================ */

TextArea {
    background: #252526;
    color: #cccccc;              /* Softer white text */
    border: none;
}

TextArea:focus {
    border-left: thick #007acc;  /* Subtle left accent */
    background: #2d2d2d;
}

TextArea .text-area--cursor {
    color: #007acc;              /* Muted blue cursor */
    background: #007acc;
}

TextArea .text-area--placeholder {
    color: #6a6a6a;              /* Very subtle placeholder */
    text-style: italic;
}

/* ============================================
   RICH LOG (Conversation Display)
   ============================================ */

RichLog {
    background: transparent;      /* Blend with parent */
    color: #cccccc;              /* Soft white */
    padding: 1 2;
}

RichLog:focus {
    background: #252526;         /* Subtle highlight */
    border: none;                /* No harsh border */
}

/* ============================================
   HEADER AND FOOTER - Minimal Style
   ============================================ */

Header {
    background: #1e1e1e;
    color: #808080;              /* Muted gray (was bright cyan) */
    padding: 0 2;
}

Footer {
    background: #252526;
    color: #6a6a6a;              /* Very subtle */
}

Footer > .footer--key {
    color: #007acc;              /* Muted blue (was bright cyan) */
    background: transparent;
}

Footer > .footer--description {
    color: #808080;
}

/* ============================================
   SCROLLBAR - Subtle and Unobtrusive
   ============================================ */

Scrollbar {
    background: #1e1e1e;
    color: #3a3d41;              /* Barely visible */
}

Scrollbar:hover {
    background: #252526;
    color: #007acc;              /* Muted blue on hover */
}

ScrollbarCorner {
    background: #1e1e1e;
}

/* ============================================
   LOADING INDICATOR
   ============================================ */

LoadingIndicator {
    color: #007acc;              /* Muted blue (was bright cyan) */
    background: transparent;
}

/* ============================================
   DESIGN NOTES
   ============================================

   Color Palette:
   - Background Primary: #1e1e1e (neutral dark gray)
   - Background Secondary: #252526 (subtle depth)
   - Background Elevated: #2d2d2d (focus states)
   - Text Primary: #cccccc (soft white)
   - Text Secondary: #808080 (muted gray)
   - Text Muted: #6a6a6a (very subtle)
   - Accent Primary: #007acc (professional blue)
   - Accent Subtle: #3a3d41 (barely visible)

   Key Changes from Previous Design:
   - ✨ Removed ALL bright cyan (#00ffff) borders
   - ✨ Borderless conversation panel (user request!)
   - ✨ Softer colors throughout (less eye strain)
   - ✨ Subtle focus indicators (left accent bar)
   - ✨ More generous padding and spacing
   - ✨ Professional, minimal aesthetic

   ============================================ */
